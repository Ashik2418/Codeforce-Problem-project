#include<bits/stdc++.h>
using namespace std;
# define int long long
#define opti ios_base::sync_with_stdio(false);
# define mise cin.tie(NULL);
signed main(){
opti mise
        int n;
        cin>>n;
        if(n == 48 || n== 49 || n== 50)
        {
            cout<<"Probin";
        }
        else if(n == 51 || n== 52 || n== 53)
        {
            cout<<"Nobin";
        }
        else
        {
            cout<<"Bohiragoto";
        }
      return 0;
}
